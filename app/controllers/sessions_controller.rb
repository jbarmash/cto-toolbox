class SessionsController < ApplicationController

	def create
		#raise request.env["omniauth.auth"].to_yaml
		#@user = User.find_by_provider_and_uid(auth["provider"], auth["uid"] || User.create_with_omniauth(auth) auth
	end
		
end
