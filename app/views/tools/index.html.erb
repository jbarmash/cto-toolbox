<h1>Tools</h1>

<center>
<% if params[:tag_name] %>
    <div class='tags'>
       Filtered By <a class="label label-info tag" ><%= params[:tag_name] %> </a> &nbsp;
       Back To <%= link_to 'All Tools', tools_path, class:'label label-info tag' %>

    </div> <!--tags-->
    
<% end %>
</center>


<%= link_to 'New Tool', new_tool_path %>
  <% @tools.each do |tool| %>
   <hr>
<div class='bit '>
<div class=''>

<!--
<div class='span8 row header'>
  <div class='vote-box'> <i bit_id='306' class='icon-arrow-up upvote'></i> <div class='vote-count'>3</div> <i bit_id='306' class='icon-arrow-down downvote'></i>
</div>
-->

 <div class='title'>
 <%= link_to tool.name, tool %> (<%= link_to 'Edit', edit_tool_path(tool) %>)  
 
 <%= link_to "Add To My Toolbox", tool_add_to_favorites_path(tool.id), :class => 'add_label' %>


    <div class='tags'>
      <% tool.tag_list.each do |tag| %>
          <%= link_to tag, tool_filter_by_tag_path(tag), :class => 'label label-info tag' %> 


      <% end %>
    </div> <!--tags-->
 </div> <!--title-->
 </div> <!--row-->
</div> <!--bit row-->

<div class='row'>
<div class='span12 code'>
   <pre class='prettyprint'> Description: <%= tool.description %> </pre>
</div>
</div>

<!--
  <tr>
    <td><%= tool.category %></td>
    <td><%= tool.public %></td>
    <td><%= tool.url %></td>
    <td><%= tool.cost %></td>
    <td><%= link_to 'Show', tool %></td>
    <td><%= link_to 'Edit', edit_tool_path(tool) %></td>
    <td><%= link_to 'Destroy', tool, confirm: 'Are you sure?', method: :delete %></td>
</tr>
-->
<% end %>
<!--
</table>
--%>

<br />

