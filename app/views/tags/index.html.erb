<h1>Tags</h1>

<table>
  <tr>
    <th>Tag</th>
  </tr>

<% @tags.each do |tag| %>
  <tr>
      <td> <%= link_to tag.name, tool_filter_by_tag_path(tag.name), :class => 'label label-info tag' %> </td>
  </tr>
<% end %>
</table>

<br />
